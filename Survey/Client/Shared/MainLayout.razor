@using Blazorise.Components
@using Survey.Client.Contract
@inherits LayoutComponentBase

@inject IUserContext userContext

<Layout>
    <LayoutHeader>
        <NavMenu IsLoggedIn="userContext.IsAuthenticated" IsAdmin="userContext.IsAdmin" />
    </LayoutHeader>
    <LayoutContent>
        @Body
    </LayoutContent>
    <LayoutFooter>
        <Footer />
    </LayoutFooter>
</Layout>

<NotificationAlert />
<MessageAlert />
<!-- Test --> 
